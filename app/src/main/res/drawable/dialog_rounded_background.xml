<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Background color inside the rounded corners -->
    <corners android:radius="30dp" /> <!-- Corner radius in dp -->
    <stroke
        android:color="@android:color/transparent"
    android:width="0dp" />
</shape>
